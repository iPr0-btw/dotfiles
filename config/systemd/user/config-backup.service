[Unit]
Description=Backup .config directory
After=network.target

[Service]
Type=oneshot
ExecStart=/bin/bash -c "cp -r ~/.config ~/.config1"
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
