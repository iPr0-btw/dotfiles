# Sway config with Hyprland-style keybinds

# Modifier key (Alt)
set $mod Mod1

# Applications
set $terminal kitty -o linux_display_server=wayland
set $fileManager nautilus
set $menu rofi -show drun

# Font
font pango:monospace 8



# Startup services
exec --no-startup-id dms run

# Volume refresh
set $refresh_i3status killall -SIGUSR1 i3status

# =====================
# MULTIMEDIA KEYBINDS
# =====================
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindsym XF86MonBrightnessUp exec --no-startup-id brightnessctl s 10%+
bindsym XF86MonBrightnessDown exec --no-startup-id brightnessctl s 10%-

# Player controls
bindsym XF86AudioNext exec --no-startup-id playerctl next
bindsym XF86AudioPause exec --no-startup-id playerctl play-pause
bindsym XF86AudioPlay exec --no-startup-id playerctl play-pause
bindsym XF86AudioPrev exec --no-startup-id playerctl previous

# =====================
# APPLICATION LAUNCHES
# =====================
bindsym $mod+q exec $terminal
bindsym $mod+b exec librewolf
bindsym $mod+e exec $fileManager
bindsym $mod+space exec $menu

# =====================
# WINDOW MANAGEMENT
# =====================
floating_modifier $mod

bindsym $mod+c kill
bindsym $mod+v floating toggle
bindsym $mod+p layout toggle split
bindsym $mod+f fullscreen toggle

# =====================
# SYSTEM CONTROLS
# =====================
bindsym $mod+Shift+m exit
bindsym Ctrl+$mod+Delete exec wlogout
bindsym Ctrl+$mod+Shift+Delete exec reboot
bindsym Mod4+l exec "swaylock -c 000000"

# =====================
# WINDOW SWITCHING
# =====================
bindsym $mod+Tab exec "swayr switch-window"
bindsym $mod+Shift+Tab exec "swayr switch-window --previous"

# =====================
# FOCUS NAVIGATION
# =====================
bindsym $mod+Left focus left
bindsym $mod+Right focus right
bindsym $mod+Up focus up
bindsym $mod+Down focus down

# Vim-style focus (optional, keep for compatibility)
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# =====================
# MOVE WINDOWS
# =====================
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Right move right
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Down move down

# Vim-style move (optional)
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# =====================
# SPLIT MODES
# =====================
# =====================
# LAYOUT MODES
# =====================
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+Shift+space floating toggle

# =====================
# CONTAINER FOCUS
# =====================
bindsym $mod+a focus parent

# =====================
# WORKSPACES
# =====================
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# Switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# Move container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10


# =====================
# SCRATCHPAD
# =====================
bindsym $mod+minus move scratchpad
bindsym $mod+Shift+minus scratchpad show

# =====================
# SCREENSHOTS
# =====================
bindsym Super+s exec "grim -g \"$(slurp)\" - | wl-copy"

# =====================
# CONFIG MANAGEMENT
# =====================
bindsym $mod+Mod4+c reload
bindsym $mod+Shift+r restart

# =====================
# RESIZE MODE
# =====================
mode "resize" {
    bindsym j resize shrink width 10 px or 10 ppt
    bindsym k resize grow height 10 px or 10 ppt
    bindsym l resize shrink height 10 px or 10 ppt
    bindsym semicolon resize grow width 10 px or 10 ppt

    bindsym Left resize shrink width 10 px or 10 ppt
    bindsym Down resize grow height 10 px or 10 ppt
    bindsym Up resize shrink height 10 px or 10 ppt
    bindsym Right resize grow width 10 px or 10 ppt

    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# =====================
# BORDERS & APPEARANCE
# =====================
default_border pixel 0
default_floating_border pixel 0



input "1267:13077:ASCE1206:00_04F3:3315_Touchpad" {
  natural_scroll enabled
}

input "1267:13077:ASCE1206:00_04F3:3315_Mouse" {
  natural_scroll enabled
}
output * scale 1
