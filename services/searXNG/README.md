## my SearXNG config

local search engine. listens on all interfaces and is configured to run on port 2222.

install the searXNG package with docker or your package manager, clone this repo, then run '(your elevation tool, like sudo) cp settings.yml /etc/searxng/'
